
import {  makeStyles, Tooltip, Grid,Typography } from '@material-ui/core';

import PageCenteredContainer from 'components/PageCenteredContainer';

const Setting = () => (
  <PageCenteredContainer>
    <Typography variant={'h1'} color={'textSecondary'}> Settings</Typography>
  </PageCenteredContainer>
);

export default Setting;
